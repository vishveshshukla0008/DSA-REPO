# JavaScript Array Problems (Easy → Hard)

---

## 🟢 EASY LEVEL

1. Return the first element of an array. ✅
2. Return the last element of an array.  ✅
3. Find the length of an array.          ✅
4. Check if a value exists in an array.  ✅
5. Sum all numbers in an array.          ✅
6. Count all even numbers in an array.   ✅
7. Reverse an array without using `reverse()`. ✅
8. Find the minimum number in an array.        ✅
9. Find the maximum number in an array.        ✅
10. Convert array to string without using `join()`. ✅

---

## 🟡 MEDIUM LEVEL

1. Remove duplicates from an array. ✅
2. Find the second largest number.  ✅
3. Count frequency of each element (return an object). ✅
4. Merge two sorted arrays (without using `sort()`). ✅
5. Rotate an array by K steps. ✅ 
6. Filter numbers greater than a given value. ✅
7. Find intersection of two arrays. ✅
8. Flatten array by one level. ✅
9. Insert an element at a given index. ✅
10. Return all positive numbers from array. ✅

---

## 🔴 HARD LEVEL

1. Implement your own `map()` function.
2. Implement your own `filter()` function.
3. Implement your own `reduce()` function.
4. Find all subsets of an array.
5. Find pair of numbers whose sum equals a target.
6. Find missing numbers in a given range.
7. Rearrange array so all even come first, then odd.
8. Find longest increasing subsequence.
9. Deep flatten a nested array.
10. Group array values by type (number/string/object).

---

## 🟣 VERY HARD LEVEL (FAANG)

1. Find all permutations of an array.
2. Find all combinations of array elements.
3. Implement binary search (recursive).
4. Implement merge sort using arrays.
5. Implement quick sort.
6. Find the maximum subarray sum (Kadane’s algorithm).
7. Find the median of two sorted arrays.
8. Implement an LRU Cache using arrays.
9. Detect cycle in an array using fast/slow pointers.
10. Find k smallest elements using heap logic.

---

